name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.88.0
      - run: cargo check --locked --all-targets
      - run: cargo test --locked --all-targets

  current:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0
          components: clippy,rustfmt
      - run: cargo +1.95.0 fmt --check
      - run: cargo +1.95.0 clippy --locked --all-targets -- -D warnings
      - run: cargo +1.95.0 test --locked --all-targets
      - run: RUSTDOCFLAGS="-D warnings" cargo +1.95.0 doc --locked --no-deps
      - run: cargo +1.95.0 test --locked --doc
      - run: cargo +1.95.0 check --locked --benches
      - run: cargo +1.95.0 bench --locked --no-run
      - run: cargo +1.95.0 package --list
      - run: cargo +1.95.0 tree --locked -e features
      - uses: EmbarkStudios/cargo-deny-action@v2
      - name: Reject non-public dependency sources
        run: |
          ! grep -E 'source = "(git\+ssh|git\+https://[^\"]*@|path\+)' Cargo.lock
